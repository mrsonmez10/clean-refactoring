function nameToObject(name) {
  var fullName = name.split(" ");
  var firstName = fullName[0];
  var lastName = lastName[1];

  var name = {
    firstName: firstName,
    lastName: lastName,
  };

  return name;
}

function sum(a, b) {
  var total = a + b;
  return total;
}

